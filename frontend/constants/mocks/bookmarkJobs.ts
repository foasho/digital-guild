import type { BookmarkJob } from "@/types";

// ブックマーク済みジョブ
export const bookmark1: BookmarkJob = {
  id: 1,
  jobId: 2, // noukaJob
  workerId: 1,
};

export const bookmarkJobs: BookmarkJob[] = [bookmark1];
